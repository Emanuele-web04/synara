# Acceptance gates — Plan 07: feedback bug reporting

| #   | Criterion                                                                                                                                                                       | CHECK                                                                                                 | RESULT                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Sidebar footer shows an icon-only "Report a bug" button (visible affordance, accessible label, bug icon)                                                                        | `cd apps/web && bun run test src/components/SidebarBugReportButton.test.tsx`                          | passed                                                                                                                                     |
| 2   | Bug button opens FeedbackDialog with Bug chip preselected (`aria-pressed="true"`)                                                                                               | `cd apps/web && bun run test src/components/FeedbackDialog.test.tsx`                                  | passed                                                                                                                                     |
| 3   | Help-menu "Send feedback" and `/feedback` open dialog with no chip preselected; plain submit payload unchanged                                                                  | `cd apps/web && bun run test src/feedbackDialogStore.test.ts src/components/FeedbackDialog.test.tsx`  | passed; `openDialog()` leaves `initialCategory` null and `feedback.ts` `submitFeedback` path unchanged                                     |
| 4   | Search palette contains "Report a bug" action that opens the dialog with Bug preselected                                                                                        | Code review + `Sidebar.tsx` action list                                                               | passed; `id: "bug"` action added to `searchPaletteActions` and routed to `openFeedbackDialog(undefined, "bug")`                            |
| 5   | Dialog shows "Draft a GitHub issue with your agent" only when category is Bug and a project exists                                                                              | `cd apps/web && bun run test src/components/FeedbackDialog.test.tsx`                                  | passed; markup contains the label only when `initialCategory="bug"` and `onDraftGithubIssue` is provided                                   |
| 6   | Clicking the agent path opens a new draft thread with prefilled prompt, focused composer, and does not auto-send                                                                | Code review of `routes/__root.tsx` + `appendComposerPromptText` in `lib/chatReferences.ts`            | passed; `handleNewThread(projectId, { fresh: true })` and `appendComposerPromptText(threadId, prompt)` used; `requestComposerFocus` called |
| 7   | Prefilled prompt contains a privacy-safe diagnostics summary and never app content (messages, prompts, file paths, logs)                                                        | `cd apps/web && bun run test src/feedbackGithubIssue.test.ts`                                         | passed; `buildBugReportDiagnostics` omits user agent, language, submitted-at; prompt contains `<diagnostics>` block                        |
| 8   | Secrets redacted and home paths rewritten in typed details and diagnostics; prompt delimiter injection prevented                                                                | `cd apps/web && bun run test src/feedbackGithubIssue.test.ts`                                         | passed; `redactObviousSecrets`, `normalizeHomePaths`, and `escapePromptDelimiters` tested                                                  |
| 9   | Agent interviews for repro / expected / actual / impact / area and shows final markdown + title before exact confirmation                                                       | `cd apps/web && bun run test src/feedbackGithubIssue.test.ts`                                         | passed; confirmation wording is exact and all template section headings are present                                                        |
| 10  | No `gh issue create` before explicit confirmation; command omits `--label`; uses `mktemp` files with `printf` and restrictive permissions; cleanup and duplicate guard included | `cd apps/web && bun run test src/feedbackGithubIssue.test.ts`                                         | passed; prompt contains `gh issue create -R ... --title ... --body-file ...`, no `--label`, and `mktemp`/`chmod 600`/`rm`                  |
| 11  | With `gh` missing/unauthenticated, agent outputs copy-ready markdown + prefilled URL and files nothing; URL over 6,000 chars falls back to title-only                           | `cd apps/web && bun run test src/feedbackGithubIssue.test.ts`                                         | passed; prompt contains fallback instructions, `GITHUB_ISSUE_URL`, and the 6,000-character warning                                         |
| 12  | Double-clicking either dialog action never submits twice or opens two threads; category chips are an accessible `role="group"`                                                  | `cd apps/web && bun run test src/components/FeedbackDialog.test.tsx` + code review of `useRef` guards | passed; `isSendingRef` and `isDraftingRef` guards added; `role="group"` present on chip container                                          |
| 13  | Focused tests and final `fmt/lint/typecheck` pass; changed-scope web tests remain green                                                                                         | `bun fmt && bun lint && bun typecheck` and `cd apps/web && bun run test`                              | all passed; lint has 473 pre-existing warnings and 0 errors; `apps/web` 330 files / 4141 tests passed                                      |
