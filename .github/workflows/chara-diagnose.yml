name: Chara diagnose Copilot ACP

on:
  push:
    branches:
      - agent/copilot-cli-acp

jobs:
  typecheck:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-workspace
      - run: bun run typecheck
